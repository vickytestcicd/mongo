name: TEST

on:
  workflow_dispatch:
    inputs:
      repository:
#         description: 'github repository'
        required: true
#         type: string
      tag: 
#         description: 'github push tag '
        required: true
#         type: string
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo  ${{ inputs.repository }}
          echo ${{ inputs.tag }}

    
